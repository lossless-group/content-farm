# content-farm — Full Corpus

> {{SITE_NAME}}

The full corpus from the content-farm pseudomonorepo: {{PLUGIN_COUNT}} plugin highlights, {{CHANGELOG_COUNT}} changelog entries, and {{CONTEXTV_COUNT}} context-v notes — rolled up from {{REPO_COUNT}} source repos and concatenated as raw markdown for LLM ingest. Each document is preceded by a metadata header (title, source, source path, canonical URL, last-modified date, kind) and separated by a horizontal rule.

See also: {{LLMS_INDEX_URL}} (link index, lighter weight).

{{CORPUS_BODIES}}
